define(function(require) {
    var endpoints = {};

    endpoints.index = function() {
    };

    endpoints.item = function(eid) {
    };

    endpoints.tree = function() {
    };

    return endpoints;
});
