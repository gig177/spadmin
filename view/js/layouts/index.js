define(function(require) {
    require('init');
    cl('hi');

/*
    var $ = require('lib/jquery'),
        app = require('app');

    $(function() {
        try {
            new app();
        } catch (e) {
            cl(e);
            cl(e.stack);
        }
    });
*/
});
